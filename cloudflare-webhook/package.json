{
  "name": "openclaw-webhook",
  "version": "1.0.0",
  "description": "Cloudflare Worker webhook service for OpenClaw bridge with WebSocket support",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^4.11.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260131.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.61.1"
  }
}
